import React from "react";

const Home = () => {
    return (
        <div className="w-full h-full">
            {/* This div serves as a container and allows the body background to show */}
        </div>
    );
};

export default Home;
